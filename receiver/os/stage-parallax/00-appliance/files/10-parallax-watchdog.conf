# Parallax OS: arm the Pi's hardware watchdog. systemd pets /dev/watchdog; if the kernel or
# systemd itself hangs, the SoC resets the board. 15 s is the bcm2835_wdt hardware maximum.
[Manager]
RuntimeWatchdogSec=15
RebootWatchdogSec=2min
